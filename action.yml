name: "Node.js Test Runner"
description: "Runs tests for a Node.js application."
author: "Tomas"
inputs:
  node-version:
    description: "The version of Node.js to use"
    required: false
    default: "18"
runs:
  using: "node16" # A versão do Node.js usada para rodar esta action
  main: "dist/index.js"

outputs:
  test-result:
    description: "The result of the test run"
branding:
  color: "blue"
  icon: "check-circle"
